package com.michu_tech.swiss_budget.frontend.desktop

fun main(args: Array<String>) {
    // TODO use jline with a custom completer and cache the network response with a short expiration time
    // TODO look into mutli threading in kotlin and make sure that not the entire console is blocked by a loading completion.
    println("Swiss Budget")
}